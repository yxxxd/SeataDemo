server:
  port: 9000

spring:
  application:
    name: business-gts-seata-example
  datasource:
    url: jdbc:mysql://localhost:3306/order?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  cloud:
    alibaba:
      seata:
        tx-service-group: my_test_tx_group